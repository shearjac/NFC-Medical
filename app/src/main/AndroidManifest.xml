<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nfcmedical">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NFCMedical">
        <activity android:name=".ModifyFullProfile">
        </activity>
        <activity android:name=".PatientOptions">
        </activity>
        <activity android:name=".WriteNFC">
        </activity>
        <activity android:name=".ReadNFC">
        </activity>
        <activity
            android:name=".FullProfileInput"
            android:label="NFC Medical">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>